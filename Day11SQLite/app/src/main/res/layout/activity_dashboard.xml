<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.w3epic.day11sqlite.DashboardActivity"
    android:padding="16dp"
    android:gravity="center_horizontal">

    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Dashboard"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:layout_marginTop="50dp"
        android:textSize="32sp"/>

    <TextView
        android:id="@+id/tvMessage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Message placeholder"
        android:layout_alignParentLeft="true"
        android:layout_below="@+id/tvTitle"
        android:layout_marginTop="50dp"
        android:layout_centerInParent="true"
        android:textSize="32sp"/>

    <Button
        android:id="@+id/btnViewUserList"
        android:text="View user list"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:onClick="btnViewUserListOnClickHandler"
        android:layout_below="@+id/tvMessage"
        />

    <Button
        android:id="@+id/btnLogout"
        android:text="Logout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:onClick="btnLogoutOnClickHandler"
        android:layout_below="@+id/tvMessage"
        android:layout_centerInParent="true"
        />

</RelativeLayout>
